on:
  push:
    branches:
      - master
  pull_request:
    types: [closed]
    branches:
      - master
jobs:
  build:
    runs-on: ubuntu-latest
    name: Update NuGet Package
    steps:
      - name: Checkout
        uses: actions/checkout@vl
